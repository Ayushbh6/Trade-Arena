# Copy to `.env` and fill with your secrets. Never commit `.env`.

### LLM / Router Keys
# Choose a provider used by your orchestration layer.
LLM_PROVIDER=openrouter

# At least one of these should be set depending on provider/tools you use.
OPENROUTER_API_KEY=sk-or-...
OPENAI_API_KEY=sk-proj-...
GEMINI_API_KEY=...
TAVILY_API_KEY=tvly-...

# OpenRouter client request timeout/retries (per HTTP call).
# Note: a full trader cycle may include multiple OpenRouter calls (tools + finalization).
OPENROUTER_TIMEOUT_S=180
OPENROUTER_MAX_RETRIES=1

# Model routing (examples). Adjust to your preferred models.
LLM_MODEL_TRADER_1=deepseek/deepseek-chat
LLM_MODEL_TRADER_2=deepseek/deepseek-chat
LLM_MODEL_TRADER_3=deepseek/deepseek-chat
LLM_MODEL_TRADER_4=deepseek/deepseek-chat
LLM_MODEL_MANAGER=deepseek/deepseek-chat
LLM_MODEL_MANAGER_FAST=
LLM_MODEL_MANAGER_THINKING=

### Embeddings / Memory
# Embedding model used for semantic memory search.
EMBEDDING_MODEL=openai/text-embedding-3-small
# Minimum cosine similarity score to keep a memory match.
MEMORY_MIN_SCORE=0.2
# Phase 7: persistent memory (raw QnA + narrative summary + grounded ledger).
# When true, traders persist per-(run_id, agent_id) memory state and use a fast summarizer to compress old chats.
MEMORY_COMPRESSION=true
# Optional summarizer model for Phase 7 narrative compression.
LLM_MODEL_SUMMARIZER=google/gemini-2.5-flash-lite
# Back-compat (older spelling used in some local envs):
LLM_MODEL_SUMMARISER=

### MongoDB
# Use either MONGODB_URI or MONGODB_URL; code should read one canonical name.
MONGODB_URI=mongodb://localhost:27017

### Binance Futures Testnet
BINANCE_TESTNET=true
# Optional override for futures testnet base URL.
BINANCE_BASE_URL=https://testnet.binancefuture.com

# If you use separate mainnet/testnet keys, keep both (execution layer will choose).
BINANCE_API_KEY=...
BINANCE_SECRET_KEY=...
BINANCE_TESTNET_API_KEY=...
BINANCE_TESTNET_SECRET_KEY=...

### Trading Config (defaults can live in src/config.py later)
TRADING_SYMBOLS=BTCUSDT,ETHUSDT,SOLUSDT
TRADING_CADENCE_MINUTES=6
